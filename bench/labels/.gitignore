# Regenerable candidate pool (large, embeds source snippets) — rebuild with
# pool_candidates.py. The raw per-persona votes are embedded in v1's `votes`.
pool.json
panel_*.json
_splits_for_tiebreak.json

# v2 labeling intermediates (regenerable / embedded in v2)
_pool_new.json
_new_reconciled.json
_splits_v2.json
panel_v2_*.json

# v3 batched-labeling intermediates (regenerable / votes embedded in v3)
batches/
tb/
panel_b*.json
tiebreak_v3_*.json

# v5 batched-labeling intermediates (regenerable / votes embedded in v5; labels in v5.json)
panel_raw_*.json
tb_batches/
tb_raw_*.json
rj_batches/
rj_raw_*.json
_splits_v3.json
_escalate_v5.json
_v4_panels/
