id = "normalize.value_graph.factor_distribute"
kind = "soundness"
summary = "The named factor_distribute rule preserves Int denotation and is gated away from free-monoid repetition."

[claim]
id = "nose.claim.normalize.value_graph.factor_distribute"

[theorem]
status = "proven"
statement = "The named factor_distribute rule preserves Int denotation and is gated away from free-monoid repetition."
model = "The Lean declarations listed in [lean] define the modeled theorem boundary."

[preconditions.integer_domain]
kind = "runtime"
status = "empirical"
summary = "Factoring is admitted only for values proven to use modeled integer multiplication and addition."
evidence = ["crates/nose-cli/tests/equivalence.rs"]

[preconditions.free_monoid_exclusion]
kind = "runtime"
status = "empirical"
summary = "String, list, and other ordered repetition domains must not enter the distributive integer rule."
evidence = ["crates/nose-cli/tests/equivalence.rs"]

[preconditions.operand_effect_safety]
kind = "runtime"
status = "empirical"
summary = "Factoring a repeated operand must not change observable evaluation count or effects."
evidence = ["crates/nose-cli/tests/equivalence.rs"]

[product]
surface = "exact-normalization"
guarantee = "Shares an exact fingerprint only when the recorded runtime preconditions admit the modeled theorem."

[evidence]
executable_tests = ["crates/nose-cli/tests/equivalence.rs"]
counterexamples = []

[rust]
rule_module = true
files = ["crates/nose-normalize/src/value_graph/rules/factor_distribute.rs"]
symbols = ["apply"]

[lean]
proof = "Proof.lean"
theorems = ["NoseFactorDistribute.distrib_sound"]
