id = "normalize.recursion.tail"
kind = "soundness"
summary = "Tail-recursive self calls and their emitted while loop reach the same final state before returning."

[claim]
id = "nose.claim.normalize.recursion.tail"

[theorem]
status = "proven"
statement = "Tail-recursive self calls and their emitted while loop reach the same final state before returning."
model = "The Lean declarations listed in [lean] define the modeled theorem boundary."

[preconditions.rust_model_correspondence]
kind = "runtime"
status = "empirical"
summary = "Rust admission must establish the modeled domain, shape, binding, and effect hypotheses before emitting this exact normalization."
evidence = ["crates/nose-cli/tests/equivalence.rs"]

[product]
surface = "exact-normalization"
guarantee = "Shares an exact fingerprint only when the recorded runtime preconditions admit the modeled theorem."

[evidence]
executable_tests = ["crates/nose-cli/tests/equivalence.rs"]
counterexamples = ["formal/obligations/normalize/recursion/tail/Counterexamples.lean#NoseRecursionTailCounterexamples.cyclic_binding_needs_hazard_safe_order"]

[rust]
files = ["crates/nose-normalize/src/recursion/tail.rs"]
symbols = ["recognize", "build_body"]

[lean]
proof = "Proof.lean"
theorems = ["NoseRecursionTail.tail_recursion_loop_sound"]
counterexamples = "Counterexamples.lean"
counterexample_theorems = ["NoseRecursionTailCounterexamples.cyclic_binding_needs_hazard_safe_order"]
