id = "normalize.control_flow.guard_returns"
kind = "soundness"
summary = "Guard-return, dead-code-after-return, ternary-return, conjoined-guard merge, and continue-guard unwrap control-flow canons preserve return/loop semantics."

[claim]
id = "nose.claim.normalize.control_flow.guard_returns"

[theorem]
status = "proven"
statement = "Guard-return, dead-code-after-return, ternary-return, conjoined-guard merge, and continue-guard unwrap control-flow canons preserve return/loop semantics."
model = "The Lean declarations listed in [lean] define the modeled theorem boundary."

[preconditions.control_flow_shape]
kind = "runtime"
status = "empirical"
summary = "Rust recognition must match the guard, exit, branch, and loop shapes represented by the control-flow model."
evidence = ["crates/nose-cli/tests/equivalence.rs"]

[preconditions.effect_and_exit_order]
kind = "runtime"
status = "empirical"
summary = "Moving or joining guards must not drop an observable effect or change which exit executes first."
evidence = ["crates/nose-cli/tests/equivalence.rs"]

[product]
surface = "exact-normalization"
guarantee = "Shares an exact fingerprint only when the recorded runtime preconditions admit the modeled theorem."

[evidence]
executable_tests = ["crates/nose-cli/tests/equivalence.rs"]
counterexamples = []

[rust]
files = [
  "crates/nose-normalize/src/value_graph/sinks.rs",
  "crates/nose-normalize/src/value_graph/control.rs",
  "crates/nose-normalize/src/value_graph/control/guards.rs",
  "crates/nose-normalize/src/cfg_norm.rs",
]
symbols = ["guarded", "process_block"]

[lean]
proof = "Proof.lean"
theorems = [
  "NoseControl.guard_clause",
  "NoseControl.guard_clause_cascade",
  "NoseControl.dead_code_after_return",
  "NoseControl.ternary_return",
  "NoseControl.conjoined_guard_merge",
  "NoseControl.continue_guard_unwrap",
]
